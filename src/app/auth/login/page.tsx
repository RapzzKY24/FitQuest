import { LoginForm } from "@/src/features/auth/components/LoginForm";
import React from "react";

const page = () => {
  return (
    <div>
      <LoginForm />
    </div>
  );
};

export default page;
